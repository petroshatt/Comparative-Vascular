run_name: unet_petct_vascular

model:
  name: unet
  in_channels: 2
  out_channels: 5
  spatial_dims: 3
  channels: [16, 32, 64, 128, 256]
  strides: [2, 2, 2, 2]
  num_res_units: 2
  dropout: 0.0

wandb:
  project: UNet-Vascular
  run_name: UNet